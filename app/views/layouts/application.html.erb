<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <title>StudyRails</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <header id = "header" class="fixed-top">
        <nav class = "navbar navbar-expand navbar-dark" style="background-color: #404146">
            <div class = "container">
	            <h5 style="color:#E8ECFF">
                    <%= link_to '[STUDY]', home_index_path, :class => 'navbar-link'%>
	            </h5>
            </div>
            <div>
              <p class="navbar-text pull-right">
              <% if user_signed_in? %>
                <strong><%= current_user.email %> 님 안녕하세요 </strong>  | 
                <%= link_to "회원정보 수정", edit_user_registration_path, :class => 'navbar-link' %>  | 
                <%= link_to "로그아웃", destroy_user_session_path, method: :delete, :class => 'navbar-link' %>
              <% else %>
                <%= link_to "회원가입", new_user_registration_path, :class => 'navbar-link' %>  | 
                <%= link_to "로그인", new_user_session_path, :class => 'navbar-link' %>
              <% end %>
              </p>
              </div>
            </div>
        </nav>
    </header>

  <body class="container-fluid wrapper">

    
    <% if notice %>
      <p class="alert alert-success"><%= notice %> </p>
    <% end%>
    
    <% if alert %>
      <p class="alert alert-danger"><%= alert %> </p>
    <% end%>
    <%= yield %>

  </body>

     <footer class="fixed-bottom" style="background-color: #404146; padding-top: 1%; margin-top: 3%">
      <div class = "container">
        <ul style="color: aliceblue; font-size: 100%; list-style: none;">
          <li style="display: inline;">&copy; 2019 COSMOS</li>
        </ul>
      </div>
    </footer>

    
</html>
