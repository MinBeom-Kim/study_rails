<%= form_with(model: user_has_study, local: true) do |form| %>
  <% if user_has_study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_has_study.errors.count, "error") %> prohibited this user_has_study from being saved:</h2>

      <ul>
      <% user_has_study.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :study_id %>
    <%= form.text_field :study_id %>
  </div>

  <div class="field">
    <%= form.label :user_role %>
    <%= form.number_field :user_role %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
