

<p>
  <strong>User name:</strong>

</p>

<table>
    <th>강의명</th>
    <% @study.each do |study| %>
    <tr>
      <td><%= study.study_name %></td>
      <td><%= link_to "스터디 신청", create_uhs_path(@user, study.id), method: :post %></td>
    </tr>
    <% end %>
</table>

<br>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>


<p></p>


