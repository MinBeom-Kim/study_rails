<p id="notice"><%= notice %></p>

<h1>Devise Tests</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @devise_tests.each do |devise_test| %>
      <tr>
        <td><%= link_to 'Show', devise_test %></td>
        <td><%= link_to 'Edit', edit_devise_test_path(devise_test) %></td>
        <td><%= link_to 'Destroy', devise_test, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Devise Test', new_devise_test_path %>
