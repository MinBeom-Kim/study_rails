<%= form_with(model: devise_test, local: true) do |form| %>
  <% if devise_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(devise_test.errors.count, "error") %> prohibited this devise_test from being saved:</h2>

      <ul>
      <% devise_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
