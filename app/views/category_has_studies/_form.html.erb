<%= form_with(model: category_has_study, local: true) do |form| %>
  <% if category_has_study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_has_study.errors.count, "error") %> prohibited this category_has_study from being saved:</h2>

      <ul>
      <% category_has_study.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :study_id %>
    <%= form.text_field :study_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
